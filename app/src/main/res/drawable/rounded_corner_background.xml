<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white" /> <!-- Or any default background color -->
    <corners android:radius="8dp" /> <!-- Adjust radius as needed for desired roundness -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" /> <!-- Optional: Add a subtle border -->
</shape>
